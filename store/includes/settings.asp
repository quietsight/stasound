<%'// Storewide Settings //

private const scVersion = "4.7"

private const scSubVersion = ""

private const scSP = "0"

private const scRegistered = "124588"

private const scCompanyName = "Sta-Sound"

private const scCompanyAddress = "4301 Will Rogers Parkway, Suite 1000"

private const scCompanyZip = "73108"

private const scCompanyCity = "Oklahoma City"

private const scCompanyState = "OK"

private const scCompanyCountry = "US"

private const scCompanyPhoneNumber = "(405) 948-3144"

private const scCompanyFaxNumber = "(405) 948-3153"

private const scCompanyLogo = "stasound_logo.png"

private const scMetaTitle = "Sta-Sound"

private const scMetaDescription = "Superior Products for Dogs, Cats, Horses, and Livestock Show Animals"

private const scMetaKeywords = "health, pets, dogs, cats, horses, livestock, sheep, goats, pigs, cattle, supplements"

private const scQtyLimit = 99

private const scAddLimit = 1000

private const scPre = 0

private const scCustPre = 0

private const scBTO = 0

private const scAPP = 0

private const scCM = 0

private const scMS = 0

private const scCatImages = 0

private const scShowStockLmt = 0

private const scOutOfStockPurchase = 0

private const scCurSign = "$"

private const scDecSign = "."

private const scDivSign = ","

private const scDateFrmt = "MM/DD/YY"

private const scMinPurchase = 0

private const scWholesaleMinPurchase = 0

private const scURLredirect = ""

private const scSSL = ""

private const scSSLUrl = ""

private const scIntSSLPage = "0"

private const scPrdRow = 3

private const scPrdRowsPerPage = 3

private const scCatRow = 2

private const scCatRowsPerPage = 2

private const bType = "H"

private const scStoreOff = "0"

private const scStoreMsg = "This store has been temporarily turned off."

private const scWL = -1

private const scTF = -1

private const scorderLevel = "0"

private const scDisplayStock = 0

private const scHideCategory = 0

private const AllowNews = 0

private const NewsCheckOut = 0

private const NewsReg = 0

private const NewsLabel = ""

private const PCOrd = 1

private const HideSortPro = 0

private const scViewPrdStyle = "C"

private const DFLabel = ""

private const DFShow = ""

private const DFReq = ""

private const TFLabel = ""

private const TFShow = ""

private const TFReq = ""

private const DTCheck = ""

private const DeliveryZip = ""

private const CustomerIPAlert = ""

private const scOrderName = "0"

private const scHideDiscField = "0"

private const scAllowSeparate = "0"

private const ReferLabel = ""

private const ViewRefer = 0

private const RefNewCheckout = 0

private const RefNewReg = 0

private const sBrandLogo = 0

private const sBrandPro = 0

private const RewardsActive = 0

private const RewardsIncludeWholesale = 0

private const RewardsPercent = 0

private const RewardsLabel = ""

private const RewardsReferral = 0

private const RewardsFlat = 0

private const RewardsFlatValue = 0

private const RewardsPerc = 0

private const RewardsPercValue = 0

private const pcQDiscountType = 0

private const iBTODisplayType = 0

private const iBTOOutofStockPurchase = 0

private const iBTOShowImage = 0

private const iBTOQuote = 0

private const iBTOQuoteSubmit = 0

private const iBTOQuoteSubmitOnly = 0

private const iBTODetLinkType = 0

private const vBTODetTxt = ""

private const iBTOPopWidth = 0

private const iBTOPopHeight = 0

private const iBTOPopImage = 0

private const scConfigPurchaseOnly = 0

private const scTerms = 0

private const scTermsShown = 0

private const scShowSKU = -1

private const scShowSmallImg = -1

private const scHideRMA = 0

private const scShowHD = 1

private const scStoreUseToolTip = 1

private const scErrorHandler = 1

private const scDisableGiftRegistry = "0"

private const scDisableDiscountCodes = "1"

private const scAllowCheckoutWR = 0

private const scSeoURLs = 0

private const scSeoURLs404 = ""

private const scQuickBuy = 0

private const scATCEnabled = 0

private const scRestoreCart = 1

private const scXML = ".3.0"

private const scGuestCheckoutOpt = 0

private const scAddThisDisplay = 0

private const scPinterestDisplay = 0

private const scPinterestCounter = "none"

private const scGoogleAnalytics = "UA-36069127-1"

'// Storewide Settings // %>

